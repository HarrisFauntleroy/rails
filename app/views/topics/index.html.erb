<h1><%= @category.name %>
  Topics</h1>

<% @topics.each do |topic| %>
  <div>
    <%= link_to topic.title, [@category, topic] %>
  </div>
<% end %>
<%= link_to "New Topic", new_category_topic_path(@category) if can?(:create, Topic) %>
