<tr>
  <td style="width:5%; text-align:center" class="nforumcaption3">
    <%= image_tag("nonew.png") %>
  </td>
  <td class="nforumcaption3">
    <%= link_to @category.name, @category, class: "category-title" %>
    <br/>
    <span class="smallblacktext">
      <%= @category.description %>
    </span>
  </td>
  <td style="width: 10%; text-align: center;" class="nforumthread">
    <%= @category.total_topics %>
  </td>
  <td style="width: 10%; text-align: center;" class="nforumthread">
    <%= @category.total_comments %>
  </td>
  <td style="width: 20%; text-align: center;" class="nforumthread">
    <span class="smallblacktext">
      <% if @category.last_comment_info %>
        <%= link_to @category.last_comment_info,
        category_topic_path(
          @category,
          @category.topics.last,
          @category.topics.last.comments.last,
        ) %>
      <% else %>
        <%= t("no_comments_yet") %>
      <% end %>
    </span>
  </td>
  <% if current_user&.admin? %>
    <td style="width: 20%; text-align: center;" class="nforumthread">
      <div class="btn-group">
        <% if policy(@category).edit? %>
          <%= link_to t("edit"), edit_category_path(@category), class: "btn" %>
        <% end %>
        <% if policy(@category).destroy? %>
          <%= button_to t("delete"),
          @category,
          class: "btn",
          method: :delete,
          data: {
            confirm: t("are_you_sure"),
            turbo_confirm: t("are_you_sure"),
          } %>
        <% end %>
      </div>
    </td>
  <% end %>
</tr>