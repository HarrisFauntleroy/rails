<% if @policy.destroy? %>
  <%= button_to t("delete"),
  @record,
  class: "btn",
  method: :delete,
  data: {
    turbo_confirm: "Are you sure?",
  },
  id: "delete_#{@record.model_name.singular}_button" %>
<% end %>
